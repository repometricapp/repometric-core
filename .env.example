# Optional: GitHub API Token
# Used to increase API rate limits (higher quota for authenticated requests)
# Public repositories only - no authentication concerns
# Leave empty to run unauthenticated
# Get token: https://github.com/settings/tokens (create "Personal access token")
GITHUB_TOKEN=

# GitHub Sources (organizations or users, comma-separated)
# Examples: "myorg,anotherorg" or "user1,user2" or "myorg,user1"
GITHUB_SOURCES=repometricapp,nooblk-98

# Optional: Explicit repositories (owner/repo, comma-separated)
# If specified, only these repos will be tracked (overrides GITHUB_SOURCES)
# GITHUB_REPOS=nooblk-98/lighthouse,kdeelz69/wordpress-image
