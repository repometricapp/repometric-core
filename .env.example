# ===============================
# Repometric Environment
# ===============================

# GitHub API Token (for private repos)
# 1. Go to: https://github.com/settings/tokens
# 2. Click "Generate new token" → "Personal access tokens"
# 3. Scopes needed: 
#    - repo (full control of private repositories)
#    - read:org (read access to org data)
# 4. Copy the token and paste it below
# Leave empty for public repos only
GITHUB_TOKEN=

# GitHub Sources (organizations or users, comma-separated)
# Mix and match orgs and users freely
# Examples: "myorg" or "user1,user2" or "myorg,user1,anotherorg"
GITHUB_SOURCES=NoobLk,nooblk-98

# Optional: Explicit repositories (owner/repo, comma-separated)
# If specified, only these repos will be tracked (overrides GITHUB_SOURCES)
# GITHUB_REPOS=owner/repo1,owner/repo2
